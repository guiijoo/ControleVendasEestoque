spring:
  datasource:
    url: jdbc:h2:mem:controle_estoque
    driver-class-name: org.h2.Driver
    username: apiestoque
    password:
  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
        ddl-auto: create # validate | update | create | create-drop
    properties:
        hibernate:
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false